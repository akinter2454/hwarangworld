# AI_RULES — 프로젝트 유지보수 규칙

1. 기존 기능을 사용자가 요청하지 않는 한 삭제하지 않는다.
2. 기존 localStorage 학생 데이터를 가능한 한 유지한다.
3. 생성형 AI API 키를 요구하는 기능을 기본 기능으로 추가하지 않는다.
4. 브라우저 코드에 비밀 키나 service_role 키를 넣지 않는다.
5. Supabase 브라우저 연결에는 publishable key만 사용한다.
6. 학생 개인정보는 최소화하고 외부 서비스로 불필요하게 전송하지 않는다.
7. 국가·문화에 대해 모든 사람을 한 가지 특성으로 일반화하지 않는다.
8. 초등학생이 터치하기 쉽고 읽기 쉬운 UI를 유지한다.
9. GitHub Pages의 프로젝트 하위 경로(`/repo/`)에서 모든 자산과 PWA가 동작해야 한다.
10. `main` 배포 전에 TypeScript/Vite build가 통과해야 한다.
11. 학교 PC 테스트는 로컬 BAT보다 GitHub Pages URL을 우선한다.
12. 오류 발생 시 앱 자체 진단센터에서 재현 정보를 수집할 수 있어야 한다.
