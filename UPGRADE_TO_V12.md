# v1.1.0 → v1.2.0 업그레이드 안내

이번 버전은 **나라별 사진·그림 노출 강화 + 설명 문구 보강** 업데이트입니다.

## 핵심 변경

- 나라별 대표 화면에 **사진·그림 요약 영역** 추가
- 각 나라 기본 시각 자료를 **실사진 4장 + 보조 그림 2장**으로 확장
- 음식 / 생활 / 문화 탭에 **관련 시각 자료 묶음** 추가
- 나라별 **식생활·생활 모습 설명을 더 구체적인 문장으로 수정**
- Service Worker 캐시 버전 갱신 (`world-tour-v12-0`)

## GitHub 반영 방법

1. 기존 저장소를 연다.
2. **`.github` 폴더는 그대로 유지**한다.
3. v1.2.0 압축을 풀어 나온 파일들로 기존 파일을 덮어쓴다.
4. `Commit changes`를 누른다.
5. GitHub Actions / Pages 배포가 끝나면 사이트를 새로고침한다.
6. 이전 화면이 남아 있으면 **강력 새로고침(Ctrl+F5)** 또는 모바일에서 캐시 새로고침을 한다.

## 확인 포인트

- 홈 화면 또는 진단센터에서 **VERSION 1.2.0** 표시 확인
- 나라 여행 진입 시 `한눈에 보는 ○○` 시각 자료 블록 표시 확인
- `음식 여행 / 생활 탐험 / 문화 탐험` 탭에서 관련 사진·그림 묶음 표시 확인
- 사진 출처가 있는 실사진 카드와 로컬 보조 그림 카드가 함께 보이는지 확인

## 데이터 호환

- 학생 학습 기록(localStorage / Supabase 구조)은 유지됩니다.
- 기존 여권, 도장, 별, 인증서 데이터는 삭제되지 않습니다.
